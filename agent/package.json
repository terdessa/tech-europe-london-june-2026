{
  "name": "flash-agent",
  "private": true,
  "version": "0.1.0",
  "description": "Flash — meeting agent runtime (P1: ears, eyes & mouth)",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "meet:login": "tsx src/login.ts",
    "typecheck": "tsc --noEmit",
    "postinstall-note": "run: npx playwright install chromium"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "playwright": "^1.47.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
