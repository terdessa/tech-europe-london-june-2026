# Flash P4 (Face) — env. Copy to .env.local. Never commit real keys.

# P2 Retrieval & Context service base URL (ingest/sources/retrieve/transcript).
CONTEXT_SERVICE_URL=http://localhost:3000

# P3 Brain (n8n) webhook base URL (agent/finalize/ask/events).
N8N_WEBHOOK_BASE=http://localhost:5678/webhook

# P1 Agent runtime base URL (dispatch the Meet bot via /join).
AGENT_URL=http://localhost:4000
